IMPORTANTE: Editar APENAS o arquivo home.tsx.

A tarefa anterior de menu hamburger NÃO foi implementada. Os links do navbar continuam visíveis em todas as telas. Preciso que você implemente o menu hamburger mobile AGORA.

O que precisa ser feito no NAVBAR/HEADER do home.tsx:

1. Adicionar um state no componente: const [mobileMenuOpen, setMobileMenuOpen] = useState(false)

2. No header/nav, MANTER o logo visível sempre. Os links de navegação (Início, Como Funciona, Depoimentos, Área do Advogado, FAQ, Blog, Termos de Uso, Área do Cliente) e o botão "Falar com Advogado" devem receber a classe "hidden md:flex" para ficarem escondidos no mobile.

3. Adicionar um botão hamburger com 3 linhas que aparece APENAS no mobile (md:hidden):
<button onClick={() => setMobileMenuOpen(!mobileMenuOpen)} className="md:hidden p-2 text-white" aria-label="Menu">
  <svg className="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
    <path strokeLinecap="round" strokeLinejoin="round" d={mobileMenuOpen ? "M6 18L18 6M6 6l12 12" : "M4 6h16M4 12h16M4 18h16"} />
  </svg>
</button>

4. Logo abaixo do header/nav, adicionar o menu mobile dropdown que abre quando mobileMenuOpen é true:
{mobileMenuOpen && (
  <div className="md:hidden fixed inset-0 top-[60px] z-50 bg-[#001532] p-6 flex flex-col gap-4 overflow-y-auto">
    <a href="/" onClick={() => setMobileMenuOpen(false)} className="text-white text-lg font-medium py-2 border-b border-blue-800/30">Início</a>
    <a href="/#como-funciona" onClick={() => setMobileMenuOpen(false)} className="text-white text-lg font-medium py-2 border-b border-blue-800/30">Como Funciona</a>
    <a href="/#depoimentos" onClick={() => setMobileMenuOpen(false)} className="text-white text-lg font-medium py-2 border-b border-blue-800/30">Depoimentos</a>
    <a href="/#faq" onClick={() => setMobileMenuOpen(false)} className="text-white text-lg font-medium py-2 border-b border-blue-800/30">FAQ</a>
    <a href="/blog" onClick={() => setMobileMenuOpen(false)} className="text-white text-lg font-medium py-2 border-b border-blue-800/30">Blog</a>
    <a href="/#avaliar" onClick={() => setMobileMenuOpen(false)} className="mt-4 block w-full text-center bg-yellow-400 hover:bg-yellow-500 text-[#001532] font-bold py-4 px-6 rounded-xl text-lg transition-all">Falar com Advogado →</a>
  </div>
)}

5. O container que agrupa os links desktop deve ter: className="hidden md:flex items-center gap-6" (ou similar)

Após concluir, faça checkpoint.